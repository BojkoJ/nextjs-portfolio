/** @type {import('next').NextConfig} */
const nextConfig = {
  // Tech-stack icons are loaded from svgl.app via plain <img>, so no
  // next/image remote patterns are required here.
};

export default nextConfig;
